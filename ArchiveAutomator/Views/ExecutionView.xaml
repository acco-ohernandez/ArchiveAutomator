<UserControl x:Class="ArchiveAutomator.Views.ExecutionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Buttons + progress bar -->
            <RowDefinition Height="*"/>     <!-- Log TextBox -->
        </Grid.RowDefinitions>

        <!-- Toolbar row -->
        <DockPanel Grid.Row="0" Margin="0 0 0 6">
            <Button DockPanel.Dock="Left" Content="▶ Start"
                    Command="{Binding StartCommand}"
                    Padding="14 4" Margin="0 0 6 0"
                    FontWeight="SemiBold"/>
            <Button DockPanel.Dock="Left" Content="⏸ Pause"
                    Command="{Binding PauseCommand}"
                    Padding="10 4" Margin="0 0 6 0"/>
            <Button DockPanel.Dock="Left" Content="✕ Cancel"
                    Command="{Binding CancelCommand}"
                    Padding="10 4" Margin="0 0 12 0"/>

            <TextBlock DockPanel.Dock="Right"
                       Text="{Binding ProgressValue, StringFormat={}{0:F0}%}"
                       VerticalAlignment="Center" Margin="6 0 0 0" Width="36" TextAlignment="Right"/>
            <ProgressBar Value="{Binding ProgressValue}"
                         Minimum="0" Maximum="100"
                         Height="18" VerticalAlignment="Center"/>
        </DockPanel>

        <!-- Log area -->
        <TextBox Grid.Row="1"
                 Text="{Binding LogText, Mode=OneWay}"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="12"
                 Background="#1E1E1E" Foreground="#D4D4D4"
                 BorderThickness="1"
                 TextWrapping="NoWrap"
                 AcceptsReturn="True">
            <TextBox.Style>
                <Style TargetType="TextBox">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding LogText}" Value="">
                            <Setter Property="Text" Value="Ready. Load a job file and click Start."/>
                            <Setter Property="Foreground" Value="Gray"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBox.Style>
        </TextBox>
    </Grid>
</UserControl>
